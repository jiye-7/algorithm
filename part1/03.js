/**
 * # 문제3 : 변수의 타입 1
 * 다음 출력 값으로 올바른 것은?
 *  1)  undefined
 *  2)  string
 *  3)  number
 *  4)  object
 */
let arr = [100, 200, 300];
console.log(typeof arr); // object

/**
 * JavaScript 변수의 타입
 * - 원시타입: number, string, boolean, undefined(값 선언은 되었지만 값 할당은 하지 않은 상태), null
 * - 참조타입: object
 */
